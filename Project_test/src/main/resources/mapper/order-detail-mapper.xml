<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.order.OrderDetailDao">

    <!-- 주문 상세 정보 저장 -->
    <insert id="save" parameterType="com.example.demo.order.OrderDetail">
        INSERT INTO orders_detail (order_no, item_no, item_name, image, detail_ea, price, review_written)
        VALUES (#{orderNo}, #{itemNo}, #{itemName}, #{image}, #{detailEa}, #{price}, #{reviewWritten})
    </insert>

    <!-- 주문 번호로 주문 상세 정보 조회 -->
    <select id="findByOrderNo" parameterType="long" resultMap="OrderDetailResultMap">
        SELECT * FROM orders_detail WHERE order_no = #{orderNo}
    </select>

    <!-- 주문 상세 정보 업데이트 -->
    <update id="update" parameterType="com.example.demo.order.OrderDetail">
        UPDATE orders_detail
        SET item_name = #{itemName}, image = #{image}, detail_ea = #{detailEa}, price = #{price}, review_written = #{reviewWritten}
        WHERE order_no = #{orderNo} AND item_no = #{itemNo}
    </update>

    <!-- 주문 번호로 주문 상세 정보 삭제 -->
    <delete id="deleteByOrderNo" parameterType="long">
        DELETE FROM orders_detail WHERE order_no = #{orderNo}
    </delete>

    <!-- 주문 상세 조회를 위한 OrderDetailResultMap -->
    <resultMap id="OrderDetailResultMap" type="com.example.demo.order.OrderDetailDto">
        <id property="orderNo" column="order_no" />
        <result property="itemNo" column="item_no" />
        <result property="itemName" column="item_name" />
        <result property="image" column="image" />
        <result property="detailEa" column="detail_ea" />
        <result property="price" column="price" />
        <result property="reviewWritten" column="review_written" />
    </resultMap>

</mapper>
